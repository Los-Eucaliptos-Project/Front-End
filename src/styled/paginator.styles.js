import styled, { keyframes } from 'styled-components';

export const StyleButtonPaginator = styled.button`
  padding: 8px 12px;
  border-radius: 4px;
  margin: 0 4px;
  cursor: pointer;
  color: darkblue;
  background-color: white;
  font-weight: 'bold';
`;